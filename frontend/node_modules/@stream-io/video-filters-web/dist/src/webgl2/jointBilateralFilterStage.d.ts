import { SegmentationParams } from '../segmentation';
export declare function buildJointBilateralFilterStage(gl: WebGL2RenderingContext, vertexShader: WebGLShader, positionBuffer: WebGLBuffer, texCoordBuffer: WebGLBuffer, inputTexture: WebGLTexture, outputTexture: WebGLTexture, canvas: HTMLCanvasElement, segmentationConfig: SegmentationParams): {
    render: () => void;
    updateSigmaSpace: (sigmaSpace: number) => void;
    updateSigmaColor: (sigmaColor: number) => void;
    cleanUp: () => void;
};

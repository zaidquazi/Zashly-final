import { TFLite } from '../tflite';
import { SegmentationParams } from '../segmentation';
export declare function buildSoftmaxStage(gl: WebGL2RenderingContext, vertexShader: WebGLShader, positionBuffer: WebGLBuffer, texCoordBuffer: WebGLBuffer, tflite: TFLite, outputTexture: WebGLTexture, segmentationConfig: SegmentationParams): {
    render: () => void;
    cleanUp: () => void;
};
